```
ssh-keygen -t rsa -C "3489274842@qq.com"
```

```
git config --global user.email "3489274842@qq.com"
git config --global user.name "SSNssn123"
```

```
git init

git add .  ---always
git commit -m "first git test"  ---always

git branch -M main
git remote add origin https://github.com/SSNssn123/firstProject.git

git push -u origin main  ---always

git pull --rebase origin master 

```